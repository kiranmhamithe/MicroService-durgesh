spring:
  application:
    name: Config-Dev
eureka:
instance:
  appname: USER-SERVICE
client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
